{
  "name": "pdf-to-markdown-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "lint": "npx web-ext lint --source-dir dist"
  },
  "dependencies": {
    "@pdf2md/core": "^0.2.0",
    "pdfjs-dist": "^5.7.284"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
